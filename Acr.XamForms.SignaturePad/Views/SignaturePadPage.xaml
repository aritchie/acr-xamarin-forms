<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Acr.XamForms.SignaturePad.Views;assembly=Acr.XamForms.SignaturePad"
			 x:Class="Acr.XamForms.SignaturePad.Views.SignaturePadPage">
    <StackLayout>
        <local:SignaturePad />
        <StackLayout Orientation="Horizontal">
            <Button Text="{Binding CancelText}" Command="{Binding Cancel}" />
            <Button Text="{Binding SaveText}" Command="{Binding Save}" />
        </StackLayout>
    </StackLayout>
</ContentPage>